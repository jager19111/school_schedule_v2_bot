BOT_TOKEN=7777777777:AAHxxxxxxxxxxxxxxxxxxxxxxx
DB_PATH=schedule_bot.db
PROXY_URL=http://10.9.9.1:7897
TIMEZONE=Asia/Novosibirsk
ADMIN_IDS=123456789,987654321
NIKA_BASE_URL=https://lyceum.nstu.ru/rasp
NIKA_REFRESH_INTERVAL_MINUTES=5 # refresh HTML source каждые 5 минут;
NIKA_COVERAGE_DAYS=21 # schedule_cache хранит текущий день + 20 будущих дней;
NIKA_HISTORY_DAYS=7 # старые уроки хранит ещё 7 дней.
HELP_PUBLIC_URL=https://example.org/schedule-bot/help
AUTHOR_CONTACT_URL=https://t.me/username
DONATION_URL=https://yoomoney.ru/000000000

# Включаем веб-модуль
WEB_ENABLED=1

# Базовый URL, который будет подставляться в кнопку (пока локальный)
WEB_PUBLIC_URL=http://10.9.9.1:8000
WEB_HOST=10.9.9.1
WEB_PORT=8000
WEB_ACCESS_MODE=public
# Секрет для генерации CSRF-токенов (сделайте длинным и случайным)
WEB_CSRF_SECRET=21313213131353654646


# === Backup ===
# бекап в тг. можно вписать как личный ID, так и ID админского чата (отрицательное число, например -100123456789). Закомментировать или 0 - только локальная копия
BACKUP_CHAT_ID=0
# === Логирование ===
# Уровень логирования (INFO для продакшена, DEBUG для разработки)
LOG_LEVEL=INFO
# Папка, в которую будут сохраняться файлы логов (app.log, errors.log, audit.log)
LOG_DIR=logs

# === Генерация постеров (ImageGenerationService, ТЗ v2.2) ===
# Глобальный рубильник: False = весь бот бесшовно работает в текстовом режиме.
ENABLE_IMAGE_GENERATION=True
# playwright | pillow (pillow — fallback для слабых серверов без Chromium).
IMAGE_RENDER_ENGINE=playwright
# Одновременных рендеров (semaphore). RAM = N контекстов Chromium;
# калибруется по RSS в htop при /stress_render.
IMAGE_MAX_CONCURRENT_RENDERS=2
# Максимум ждущих в очереди; сверх — мгновенный текстовый fallback.
IMAGE_QUEUE_CAPACITY=3
# Общий бюджет запроса: ожидание слота + рендер, секунды.
# Калибровка (этап 5): >= p99_рендера + QUEUE_CAPACITY / (рендеров_в_сек).
IMAGE_RENDER_TIMEOUT_SEC=4.0
# Рецикл браузера: не более N рендеров на процесс...
IMAGE_BROWSER_RECYCLE_RENDERS=200
# ...и не старше N минут (что наступит раньше).
IMAGE_BROWSER_RECYCLE_INTERVAL_MIN=720
# Ширина постера, px. 1080 переживает JPEG-компрессию Telegram.
POSTER_WIDTH=1080
# Rate limit: генераций на пользователя за окно. Расходуется только
# на фактический рендер; кэш-хиты и warm-up лимит не расходуют.
IMAGE_RATE_LIMIT_MAX=10
IMAGE_RATE_LIMIT_WINDOW_SEC=180
# L1 (PNG-байты): TTL записей и предел числа записей (RAM ~0.1 МБ/запись).
IMAGE_CACHE_TTL_HOURS=24
IMAGE_CACHE_MAXSIZE=200
# L2 (file_id): строки, память копеечная — лимит можно держать много выше L1.
IMAGE_FILE_ID_CACHE_MAXSIZE=2000
# Circuit breaker: серия ошибок рендера до размыкания цепи
# и пауза (сек) перед пробной попыткой half-open.
IMAGE_BREAKER_FAILURE_THRESHOLD=5
IMAGE_BREAKER_COOLDOWN_SEC=120
